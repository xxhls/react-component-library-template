{
	"name": "@tmp/demo-dist",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "bash ./cp-dist && vite",
		"build": "tsc -b && vite build",
		"preview": "vite preview"
	}
}
